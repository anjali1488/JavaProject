package banktest;

public interface StateBankInterfac {
	//As multiple inheritance is not used in Java we can have interfaces.
	//An interface can have both abstract and concrete methods(any method with implementation)
	//Interface need not to be made abstract
	//Interface cant be instantiated.
	public abstract double addBonus();
}
